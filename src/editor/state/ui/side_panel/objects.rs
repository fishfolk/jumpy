use macroquad::prelude::collections::storage;

use crate::{
    editor::{
        actions::{UiAction, UiActionExt},
        state::EditorTool,
        util::EguiTextureHandler,
    },
    resources::TextureResource,
    Resources,
};

use super::Editor;

impl Editor {
    pub(super) fn draw_object_info(&mut self, ui: &egui::Ui) {
        // TODO
    }
}
